- defaultTab: nodes
  description: |-
        Path: https://github.mpi-internal.com/Yapo/data-revenues/tree/master/incremental-product-order
        sudo docker run --rm --net=host \
                    -v /home/bnbiuser/secrets/blocket_db:/app/db-secret \
                    -v /home/bnbiuser/secrets/dw_db:/app/dw-secret \
                    -e APP_DB_SECRET=/app/db-secret \
                    -e APP_DW_SECRET=/app/dw-secret \
                    containers.mpi-internal.com/yapo/incremental-product-order:latest
  executionEnabled: true
  group: CORE
  loglevel: DEBUG
  name: Core - Incremental Products Order
  nodeFilterEditable: false
  nodefilters:
    dispatch:
      excludePrecedence: true
      keepgoing: false
      rankOrder: ascending
      successOnEmptyNodeFilter: false
      threadcount: '1'
    filter: 'name: dockerhost'
  nodesSelectedByDefault: true
  notification:
    onfailure:
      email:
        recipients: data_team@adevinta.com
        subject: ${job.execid} ${execution.status} ${job.name} - Fallo definitivo
      plugin:
        configuration:
          slack_channel: ''
          webhook_base_url: https://hooks.slack.com/services
          webhook_token: T017F9KHA1Y/B01BL7C1CSY/Ai9NzdCrBUA5Ru5sa8JHYrjR
        type: SlackNotification
  notifyAvgDurationThreshold: null
  scheduleEnabled: false
  sequence:
    commands:
    - script: |-
        sudo docker pull containers.mpi-internal.com/yapo/incremental-product-order:latest
        sudo docker run --rm --net=host \
                        -v /home/bnbiuser/secrets/blocket_db:/app/db-secret \
                        -v /home/bnbiuser/secrets/dw_db:/app/dw-secret \
                        -e APP_DB_SECRET=/app/db-secret \
                        -e APP_DW_SECRET=/app/dw-secret \
                        containers.mpi-internal.com/yapo/incremental-product-order:latest
    keepgoing: false
    strategy: node-first
  timeZone: America/Santiago
